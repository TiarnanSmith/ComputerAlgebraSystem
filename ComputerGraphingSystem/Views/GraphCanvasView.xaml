<UserControl x:Class="ComputerGraphingSystem.Views.GraphCanvasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="1280" d:DesignWidth="720">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <!--TThe Side Input-->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="1" HorizontalAlignment="Stretch">
                <ListView ItemsSource="{Binding ViewControls}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="3*"/>
                                </Grid.ColumnDefinitions>
                                <Label Grid.Column="0" Content="{Binding Description}"/>
                                <TextBox Text="{Binding TBContents, UpdateSourceTrigger=PropertyChanged}" 
                                     Grid.Column="1" MinWidth="50"
                                     PreviewTextInput="NumberValidationTextBox"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

            <Grid Grid.Row="0" HorizontalAlignment="Stretch">
                <ListView ItemsSource="{Binding EquationControls}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="3*"/>
                                </Grid.ColumnDefinitions>
                                <Label Grid.Column="0" Content="{Binding Description}"/>
                                <TextBox Text="{Binding TBContents, UpdateSourceTrigger=PropertyChanged}" 
                                     Grid.Column="1" MinWidth="50"
                                     PreviewTextInput="NumberValidationTextBox"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>
        

        <Grid Grid.Column="1" HorizontalAlignment="Stretch" 
          VerticalAlignment="Stretch"
            Height="720" 
            Width="1280" 
            Margin="0 0 0 0"
          Background="BlanchedAlmond">
            <Grid>
                <!--Coordinate Labels-->
                <!--Left, Top, Right and Bottom (Margins)-->
                <Grid>
                    <Grid.Resources>
                        <Style TargetType="Label">
                            <Setter Property="Foreground" Value="Blue"/>
                            <Setter Property="FontFamily" Value="Century Gothic"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </Grid.Resources>


                    <Label Content="MOPYGraph" HorizontalAlignment="Left" VerticalAlignment="Top"/>

                    <Label Content="{Binding Path=GraphSystem.PlotView.XStartValue}"  HorizontalAlignment="Left" VerticalAlignment="Center" Padding="0 50 0 0"/>
                    <Label Content="{Binding Path=GraphSystem.PlotView.XEndValue}"  HorizontalAlignment="Right" VerticalAlignment="Center" Padding="0 50 0 0"/>

                    <Label Content="{Binding Path=GraphSystem.PlotView.YStartValue}"  HorizontalAlignment="Center" VerticalAlignment="Bottom" Padding="0 0 50 0"/>
                    <Label Content="{Binding Path=GraphSystem.PlotView.YEndValue}"  HorizontalAlignment="Center" VerticalAlignment="Top" Padding="0 0 50 0"/>
                </Grid>

                <!--Handles control of mutiple curves.lines-->
                <Line X1="0" X2="1280" Y1="360" Y2="360" Stroke="Gray" StrokeThickness="2"/>
                <Line X1="640" X2="640" Y1="0" Y2="720" Stroke="Gray" StrokeThickness="2"/>

                <Grid>
                    <Grid.Resources>
                        <SolidColorBrush x:Key="CurveGrey">#FFF56111</SolidColorBrush>
                    </Grid.Resources>

                    <Border>
                        <!--https://stackoverflow.com/a/69810928/17302244-->
                        <Border.Clip>
                            <RectangleGeometry Rect="0, 0, 1280, 720" RadiusX="10" RadiusY="10"/>
                        </Border.Clip>
                        
                        <Grid>
                            <Grid.RenderTransform>
                                <TranslateTransform X="640" Y="360"/>
                            </Grid.RenderTransform>

                            <ItemsControl ItemsSource="{Binding CurveLines, UpdateSourceTrigger=PropertyChanged}">
                                <!--Wraping: https://stackoverflow.com/a/3131962/17302244-->

                                <ItemsControl.ItemContainerStyle>
                                    <Style>
                                        <Setter Property="FrameworkElement.Margin" Value="0"/>
                                    </Style>
                                </ItemsControl.ItemContainerStyle>

                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <Grid Grid.ColumnSpan="0" Grid.RowSpan="0">

                                        </Grid>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Polyline Points="{Binding Points, UpdateSourceTrigger=PropertyChanged}"
                                          Stroke="{StaticResource ResourceKey=CurveGrey}" Opacity="0.5"
                                          StrokeThickness="{Binding Width, UpdateSourceTrigger=PropertyChanged}"
                                          Margin="0 0 0 0"
                                          Tag="{Binding Name}" 
                                          />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
